# selene-editor-shared

Shared protocol, document payloads, and preview-bridge contracts used by the Selene editor frontend, service, and specs.

## Check

```bash
moon check --target all --deny-warn --manifest-path moon.mod.json
```
