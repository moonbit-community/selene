# ISS-004: Batch textured primitives and cache material bind groups

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: webgpu, render3d, performance, materials
- Assignee: codex
- Created: 2026-06-01
- Updated: 2026-06-01
- External ref: https://github.com/moonbit-community/selene/issues/35

## Description
Extend the WebGPU 3D batching path to textured primitives and repeated material bindings. The current textured path builds triangle meshes and creates bind groups in the render path, which is expensive for repeated textured objects.

## Design
Reuse the primitive GPU mesh cache from ISS-002 and the instance submission model from ISS-003. Add material/texture bucket keys that include base, emissive, metallic-roughness, occlusion, normal textures, sampler codes, double-sided state, and shadow state. Cache bind groups for stable material texture combinations and shadow texture state where safe.

## Acceptance Criteria
- [x] Textured built-in primitives avoid per-item primitive mesh generation when their parameter set is cacheable.
- [x] Compatible textured primitives are bucketed to reduce draw calls where possible.
- [x] Stable material texture bind groups are reused instead of being recreated for every compatible command.
- [x] Existing textured primitive, normal, tangent, alpha, and double-sided tests continue to pass.
- [x] `moon test selene-webgpu/src --target js --deny-warn` passes.

## Relationships
- Depends on: ISS-003
- Parent: none
- Related: ISS-005
- Discovered from: none

## Notes
- 2026-06-01: Created from the analysis of GitHub issue 35.
- 2026-06-01: Added the first ISS-004 slice: per-frame resource-key caching for lit and textured 3D bind groups. Textured primitive instancing remains open because it requires a submission protocol change for textured built-in primitives, not just a render loop cache.

## Close Notes
- 2026-06-01: Added generated mesh template caching for textured built-in cuboid/plane, sphere, and cylinder primitives. Added compatible textured command compaction by texture views, sampler codes, double-sided state, and cast-shadow state, so repeated textured primitives can render with fewer draw calls while preserving transformed vertex data. Existing per-frame material bind group caching remains in place. Verified with `moon test selene-webgpu/src --target js --deny-warn` and `moon check --target all --deny-warn`.
